{
  "name": "golden-lady",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --include=dev --prefix server && npm install --include=dev --prefix client && npm install --include=dev --prefix admin",
    "dev:server": "npm start --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin",
    "build": "npm run build --prefix client && npm run build --prefix admin",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
